-- dart-class-tools.nvim auto-load
-- This file is loaded automatically by Neovim's plugin system.
-- The actual setup is done via require("dart-class-tools").setup()
-- which should be called from the user's Lazy.nvim config.

if vim.g.loaded_dart_class_tools then
  return
end
vim.g.loaded_dart_class_tools = 1
